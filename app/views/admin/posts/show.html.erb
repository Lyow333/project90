<div class="container">
	<div class="row">
		<div class="col-md-6">
			<% if @post.image.present? %>
			<%= image_tag @post.image, width: '100%' %>
			<% end %>
		</div>
		<div class="col-md-6">
			<h2><%= @post.title %></h2>
			<p><%= @post.content %></p>
		</div>
	</div>
</div>